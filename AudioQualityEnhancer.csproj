<Project Sdk="Microsoft.NET.Sdk">

  <!-- Shared settings live in Directory.Build.props. -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <!-- Keep in sync with the release tag when stamping a new version. -->
    <Version>0.17.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="AudioQualityEnhancer.Tests\**\*" />
    <Compile Remove="Temp\**\*" />
    <EmbeddedResource Remove="AudioQualityEnhancer.Tests\**\*" />
    <EmbeddedResource Remove="Temp\**\*" />
    <None Remove="AudioQualityEnhancer.Tests\**\*" />
    <None Remove="Temp\**\*" />
    <Page Remove="AudioQualityEnhancer.Tests\**\*" />
    <Page Remove="Temp\**\*" />
  </ItemGroup>

</Project>
